{
  "name": "overdo-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "lint": "bunx eslint .",
    "test": "bunx vitest run --config vitest.config.ts"
  }
}
